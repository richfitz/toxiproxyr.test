language: r
cache: packages
sudo: false

services:
  - postgresql
addons:
  postgresql: "9.4"
  apt:
    packages:
      - libpq-dev

before_script:
  - Rscript -e 'toxiproxyr:::copy_scripts(".")' && ./toxiproxy_start.R .
